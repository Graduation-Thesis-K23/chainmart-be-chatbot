version: '3.0'
services:
  rasa:
    build: .
    networks:
      - rasa-network

  action_server:
    build: ./actions
    networks:
      - rasa-network

  socketio:
    build: ./socket-io
    ports:
      - 5000:5000
    networks:
      - rasa-network

networks:
  rasa-network:
    driver: bridge
